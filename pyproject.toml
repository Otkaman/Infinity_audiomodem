[project]
name = "infinity-audiomodem"
version = "0.1.0"
description = "Передача файлов через звук (акустический модем)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "sounddevice>=0.5.5",
    "scipy>=1.14.0",
]
